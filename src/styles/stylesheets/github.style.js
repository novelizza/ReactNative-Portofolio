import {StyleSheet} from 'react-native';
import Color from '../colors';

export default StyleSheet.create({
  container: {
    backgroundColor: Color.ORANGE_COLOR,
    flex: 1,
  },
  githubLogo: {
    alignSelf: 'center',
    height: 180,
    width: 180,
  },
});
